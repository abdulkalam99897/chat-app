Expo mobile client:
- Ensure server is running on http://localhost:5000
- Install dependencies: npm install
- Install AsyncStorage (required): expo install @react-native-async-storage/async-storage
- Start app: npm start (or expo start)
- For physical device, replace 'localhost' with your machine IP in App.js and screen files.
